version: '3'

networks:
     laravel:

services:
    nginx:
        depends_on:
            - php
            - mysql
        networks:
            - laravel:

    mysql:
        networks:
            - laravel:

    php:
        networks:
            - laravel:
