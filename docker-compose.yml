version: '3'

networks:
    -laravel:

services:
    nginx:
        networks:
            -laravel:
                -
    mysql:
        networks:
            -laravel:
                -
    php:
        networks:
            -laravel:
